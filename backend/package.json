{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend Pablo Sgreccia - W3 IT Solutions",
  "main": "app.js",
  "scripts": {
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --detectOpenHandles",
    "inicio": "npm i && npx sequelize-cli db:create && npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "dev": "nodemon app.js",
    "start": "node app.js"
  },
  "author": "Pablo Sgreccia",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^3.1.2",
    "sequelize": "^6.28.0"
  },
  "devDependencies": {
    "jest": "^29.4.2",
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.6.0",
    "supertest": "^6.3.3"
  }
}
